class RegisterAnimationConfigs {
  const RegisterAnimationConfigs._();

  static const completedFormDelayDuration = Duration(milliseconds: 400);
  static const animatedDuration = Duration(milliseconds: 500);
  static const completedNextButtonDuration = Duration(milliseconds: 250);
  static const thankYouTextTranslationDuration = Duration(milliseconds: 250);
  static const thankYouTextDelayDuration = Duration(milliseconds: 650);
}
