import 'package:flutter/material.dart';

class CompletedFormWD extends StatelessWidget {
  const CompletedFormWD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
