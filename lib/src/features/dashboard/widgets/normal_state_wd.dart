import 'package:flutter/material.dart';

class NormalStateWD extends StatelessWidget {
  const NormalStateWD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
