class ACDACommonMessages {
  const ACDACommonMessages._();

  // Camera Options
  static const galleryOption = 'Gallery';
  static const cameraOption = 'Camera';

  // Options
  static const close = 'Cancel';
}
